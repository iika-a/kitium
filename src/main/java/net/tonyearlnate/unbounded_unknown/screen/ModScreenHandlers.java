/*package net.tonyearlnate.unbounded_unknown.screen;

import net.minecraft.registry.Registries;
import net.minecraft.registry.Registry;
import net.minecraft.resource.featuretoggle.FeatureSet;
import net.minecraft.screen.ScreenHandler;
import net.minecraft.screen.ScreenHandlerType;
import net.minecraft.util.Identifier;
import net.tonyearlnate.unbounded_unknown.LightningScreenHandler;
import net.tonyearlnate.unbounded_unknown.UnboundedUnknown;

public class ModScreenHandlers {
    public static final ScreenHandler LIGHTNING_SCREEN_HANDLER = Registry
            .register(Registries.SCREEN_HANDLER, Identifier.of(UnboundedUnknown.MOD_ID, "lightning_screen_handler"), ScreenHandlerType(LightningScreenHandler::new, FeatureSet.empty()));

    public static void registerModScreenHandlers() {
        UnboundedUnknown.LOGGER.info("Registering Screen Handlers for " + UnboundedUnknown.MOD_ID);
    }
}*/
